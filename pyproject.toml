[project]
name = "hyperliquid-dilution-edge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "NickMeret2004", email = "nickmeretsky@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "hyperliquid>=0.4.66",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
hyperliquid-dilution-edge = "hyperliquid_dilution_edge:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
